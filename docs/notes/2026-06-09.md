# 2026-06-09 — composed_of re-validate + full re-merge

Continuation of the 2026-06-08 nominal-discriminator thread. Two parts the
user asked for in one directive: «(re-apply + фінальний dry-run на залишкових
edge, тоді full re-merge) і re-validate-прохід (KM 42 — найвищий leverage)».

## The sticky-absorb problem (re-validate)

The cross-source MERGE stage (`merge_seeds_cross_source`) re-runs union-find
from scratch each time, so the shipped nominal discriminator separates
wrong-nominal coins there correctly. But the ABSORB stage
(`absorb_seeds_into_final_v2`) is additive + STICKY: once a unified entry is
in a foundation's `composed_of`, no later run re-validates it. So mis-
groupings that predate the discriminator (and V1-bootstrap composed_of)
survive forever. KM 42 «8 Skilling» (`dk-tid-163034`) carried «1 Denning»
(0.44 g, `unified-kmk-137199`) + «4 Skilling lybsk» (1.822 g,
`unified-kmk-294714`) + two «6 Skilling» — verified via direct
`match_pair(member, foundation)` returning `no_match`.

### Why a NEW pass, not the existing over-merge purge

The over-merge purge (§9.4) handles a curator-reviewed allowlist of
DIFFERENT-base-KM evictions, requires a clean foundation TWIN (resets the
host's accumulated fields to it), and 6 of my 12 KM-42-class hosts are
curator-synthetic ids with NO twin. So I built a separate criterion-driven
`_revalidate_composed_of`.

### The two design traps caught

1. **Weight-tier would false-drop specimens.** Running the FULL `match_pair`
   over existing members gave 38 drops — but 24 were SAME-nominal, dropped
   only by the weight-tier-1 disambiguator (Δ>5 %). That tier is for SPLITTING
   different coins at merge time; re-applied to membership it kills legitimate
   specimen variance (a worn / off-standard piece). Fix: re-validate on
   IDENTITY only — genuine nominal differ + no type-level catalogue tie. Drops
   12, all genuinely different nominals; royal_holstein + holstein_schauenburg
   (whose 8 drops were all same-nominal weight) correctly cleared.

2. **Two `_normalise_nominal` functions.** absorb imports the BARE normaliser
   from `lib.v2_seed_writer` (no synonym table) AND `match_pair` from the merge
   module (full synonym table). My first dry-run used the bare one → 18 drops
   including FALSE «1 Ducat» vs «1 Dukat», «2 Ducat» vs «1 Dobbelt Dukat»,
   «1 Gold Krone» vs «1 Guldkrone». Fix: import the merge module's normaliser
   as `_mg_normalise_nominal` for the identity check (consistency with the
   shipped discriminator). 18 → 12, all genuine.

### Decontamination is twin-independent (surgical-subtract)

Prior enrichment bakes every member's weight/sources onto the foundation
(`members[0]==fc`), so dropping a member isn't enough — its 0.44 g survives on
fc and re-pollutes. The over-merge purge resets to a twin; my hosts often have
none. Verified the no-twin hosts' accumulated data is PURELY member-derived
(km-56/km-582: 0 own-only sources; km-74: 4 ORPHAN sources backed by no current
member). So `_surgical_decontaminate` removes only values contributed by an
evicted member AND by NO remaining member — orphan + foundation-own data
preserved (§9a). Year left alone (overshoot is the §0-safe direction).

Re-home reuses the over-merge purge's `forced_evict_promote` set →
bulk-promote path (default mode `no_basic_peer_only` makes it fire). danish_realm:
12 evictions / 8 hosts → 10 standalone + 2 matched into correct existing finals
(`unified-dk-hede-c4h35` ½ Dukat, `unified-dk-hede-c4h99a` 1 Mark); 0 lost.

## Residual-edge nominal folds

Issuer-PREFIX («Oldenburg. Taler» → daler) was already handled by
`_strip_region_prefixes`. Added two folds for the remaining shapes: worth-
equivalence tail (`= …` strip — «1 Thaler = 1/14 Cölln. Mark» → daler) +
`_strip_mint_suffix` (trailing mint-only «. København og Altona» → ""). Both
conservative; «100 Rd. Conr.» / «Cölln. Mark» untouched; full regression set
clean.

## Full re-merge — materialise everywhere

Prior discriminator ship re-merged only 4 entities. Full sweep: 26651 seeds →
17337 unified (9314 merges). **Seed-conservation check across all 22 entities:
ZERO lost.** Only `_unclassified` +322 (new harvest seeds entering the merge →
classification backlog). 9 unified ids «vanished» from finals but every
underlying SEED traced to a surviving final entry (unified ids are derived +
re-grouped under different anchors). KM 42 idempotently clean post-pipeline.

## Commits (3, unpushed)

- `51a609c` build: re-validate composed_of membership (KM 42 fix)
- `1d08444` build: mint-suffix + worth-equivalence nominal folds
- `2fcde35` data: full re-merge + re-absorb (22 seed_unified + 5 final + 19 cd)

## Open threads

- `_unclassified` +322 awaiting entity routing.
- Transitivity-aware re-home (re-home a dropped member that itself anchors a
  sub-cluster) deferred — regression-prone, own session.
- audit_v2 I4 (seed_unified `white-metal` schema) + I6 (merge_decisions unknown
  ids `dk-hede-f5h12` / `c4h112`) are PRE-EXISTING (untouched files), not from
  this task; flagged for a future cleanup.

## Davenport / multi-value catalog-index normalization (later same day)

User re-flagged un-normalized dav: «Dav# 3679 / 3679A, EC II 3679, EC II 3679A».
Diagnosed FOUR royal_holstein «4 Mark» Christian V entries; the ucoin one
(`unified-km-77-1-chr-v-1686`, fuss=seed_unsorted) sat unclassified BESIDE its
Hede twin `unified-dk-hede-c5h125a` despite sharing numista 108760 + km 77.
Root cause = the slash-multi «hede: 125A / 125B» / «dav: 3679 / 3679A» was a
single un-split string: `_catalog_refs` saw «125A / 125B» which equalled
neither «125A» nor «125B» → catalog no-overlap → merge blocked. The display
gap AND the merge-blocker were the SAME bug.

Three normaliser gaps + one matcher gap fixed (`a4d2411`):
- catalog_codes: strip stray dav volume «#» («EC II#»→«EC II», 50+); generalise
  slash-split from km-only to every list-capable field; fold «Dav EC II# N»
  others-token via map_catalog_token; promote bare→single-volume
  («[EC II 3679, 3679A]»→«[EC II 3679, EC II 3679A]»).
- merge `_catalog_refs._split_multi`: split «A / B» when building match refs
  (hede/schou/sieg + single-source fields) so the matcher sees each sub-variant.
  match_pair(e4,e3) → confident 4/4.

Materialized via full re-merge + re-absorb (`badf427`). Dataset-wide residual
dav with «#» = 0, with «/» = 0; no slash-multi left in ANY index field. Zero
seeds lost (seed-set unchanged everywhere — only dav CONTENT changed); only
royal_holstein final −1 (the e4→e3 absorb). The merged entry now reads
`dav: [EC II 3679, EC II 3679A, EC II 3680]`.

Note: km-108 / km-110 «4 Mark» stay SEPARATE from the km-77/83 cluster — a
genuine different-Krause-edition split (both render clean «EC II 3679»), not a
normalization miss.
