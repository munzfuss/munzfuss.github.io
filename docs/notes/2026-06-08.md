# 2026-06-08 — Catalogue-index normalization + KMM citation thinning

Began as the «1 Speciedaler Christian IV (Hede 55)» 3-problem ticket
(Schou case-insensitive / anomalous weights 58.90 + 1.89 / no thinning
effect) and grew, under the user's directives, into a project-wide
catalogue-index refactor + a museum-citation render-declutter. Five
commits, all UNPUSHED.

## What happened

**Root cause of the Hede 55 anomalies** — a mis-merge. The two anomalous
weights came from Wolfenbüttel occupation-coinage KMM records (290903
58.9 g, 291098 1.89 g «gutergros») that the absorb had stuck into the
København Speciedaler via a bare «Schou 4/5/14» collision. The current
weight-tier-1 matcher already rejects them; they were sticky pre-2026-05-22
residue. Removed from `dk-tid-97358` composed_of + phantom source URLs
surgically cleaned. KMM 290904 (PRO RELIGIONE 1627 Wolfenbüttel, 28.35 g)
stays pending the user's split verdict (nested with the real Hede 55
KMM 348808). The «thinning shows no effect» was correct behaviour — the
weights were legitimately spread across sub-variants.

**(A) Catalogue-index normalization** (`17c7e91` code, `75734e6` data).
`normalise_catalog()` (case-insensitive fold of `others: <code># N` →
typed list-field + case-insensitive value de-dup) wired into all 4
catalog-write chokepoints. The big one: a **restart-scope registry** in
`_catalog_refs` — empirically Hede 59 % / Schou 64 % / Sieg 42 % of values
collide across reigns → ruler-scoped; KM 43 % cross-entity → register;
Galster per-vol; rest global. **Sieg + Schou were bare = a §9.4 cross-reign
false-merge bug; fixed.** Rollout to all entities: 0 cross-ruler false
merges, 66 §9a museum-specimen consolidations, contained to the 3 Danish
entities.

**(B) natmus errata** (`18a5fbe`). KMM 275643 «2 Skilling 1625» wrongly
typeNumber'd «Hede 141» (= 8 Skilling 1630); corrected to Hede 134 via
`_source_errata`, corroborated by KMM 190547 (real Hede 141) + KMM 335046
(natmus's own «H. 134A» sibling) + danskmoent.

**(C) KMM citation thinning** (`7d37a92` code, `758cfba` data). 3-category
declutter: weight → untouched (§9a owns it); image-only → keep 3; neither
→ keep 1. Image presence read from cache `related.assets[type=still]`,
verified live == the natmus page. 3266 surplus hidden via `display: false`
(data kept). 79 % of all KMM cites are the bare «neither» kind.

**Docs.** Made handoff-update at session end MANDATORY (CLAUDE.md + PB-8
step 3, user direction). Logged the scope registry, KMM thinning, and the
275643 errata in DECISIONS.md; documented image/weight cache-detection +
thinning in SOURCES §8.3.

## §9a weight-thinning → pipeline (later same day)

User picked weight-thinning as the next build (`роби`), design FINAL =
option 1: hide the over-collected intermediate weight readings + their
citations via `display:false` (data kept); keep min/middle/max; catalog
refs untouched. Shipped in 3 parts:

- **Schema** (`fb91804`): `FieldValue.display:bool=True`.
- **compute** (`fb91804`): `normalise_field` skips `display:false` readings
  → excluded from the rendered weight/fineness column AND derived Feingewicht.
- **absorb** (`fb91804`): `_suppress_weightless_museum_overcollection` now
  thins the weight-giving KMM bucket too. When ≥5 weight-giving KMM specimens
  pile up (all share absent fineness → uniform per §9a), keep min / pos-len//2
  / max by weight; set display:false on the dropped specimens' citations AND
  (matched by value, since KMM weight entries are `{value, "kmk"}` with no nid)
  their `weight_rough_g` readings. New `_kmm_specimen_weight(nid)` cache getter.

Applied via `absorb --apply` to the 3 KMM-over-collecting entities
(`264c4a8`): danish_realm 1018 / danish_norway 262 / royal_holstein 40 =
1320 `display:false`, **0 deletions**, no reformatting, no other churn (the
emit is diff-stable). Verified end-to-end: KM-42 (`dk-tid-163034`, 8 Skilling
Christian IV) weight column **44 → 5** readings (kmk min/mid/max 0,44 / 2,66 /
3,13 + ucoin 2,92 + numista 2,923); catalog Hede 93A/96/93/93B intact.

**Caveat surfaced (expected):** the §9a envelope keeps the EXTREMES, so KM-42's
displayed min 0,44 g is the sticky «Denning» anomaly (KMM 137199, wrong type).
Thinning correctly surfaces it — it didn't introduce it. → the re-validate-
composed_of absorb pass (handoff OPEN, highest leverage) is now also the
clean-up for the thinned envelope: it drops the Denning + 4-Sk-Lybsk via the
existing weight-tier-1 discriminator, leaving a clean min/mid/max.

## Rhinsk Gylden = wrong Fuß (later same day)

User: «Rhinsk Gylden → перенести в rhinsk_gylden_fod (Δ −1,7%, чисто). роби».
Investigation (§0b — verified, didn't trust handoff): c3h14 «1 Rhinsk Gylden»
+ c3h15 «2 Rhinsk Gylden», Christian III 1546 Flensborg, **gold** .75, were
sitting in the **silver** `8_daler_lybsk_fod`. Root cause: the Flensborg
mint-anchor rule in `auto_classify_seed_unsorted.py::_MINT_BOUND_FUSSES`
matched mint+ruler+year WITHOUT a metal check, and its `kind_by_nominal`
even deliberately mapped «Rhinsk Gylden»→kurant — so it swallowed the gold
pieces into the silver Flensborg line. §8a step-1 (metal-mismatch) was the
missing filter.

Fix in two commits:
- **`896ffef` (classifier)** — `allowed_metals` gate on the bindings +
  `_coin_matches_mint_binding` rejects metal-outside BEFORE mint/ruler/year.
  8_daler_lybsk_fod ← {silver, billon}. Systemic; future gold won't mis-route.
- **`2dc3adf` (data)** — moved c3h14/c3h15 → `rhinsk_gylden_fod` phase 0 (the
  fuss ALREADY had a Christian III Phase 0 = 1536 Roskilde galster-c3g-131, so
  the 1546 Flensburg pair extends it, not a new phase). Added fraction '2'
  (soll 6.496 rauh / 5.002 fein; c3h15 fein 4.917 → Δ −1.7 %, same as c3h14
  fraction 1 → Δ −1.7 %). New ref `danskmoent-c3-rhinsk-gylden-1546` (Galster
  130, NFM XII s.10). denmark.yml Phase-0 prose extended across all 3 surfaces
  (timeline-bar year_to 1536→1546 + title + description; fuss_periods
  hintergrund; pdate_label hiatus 27y→17y) — sourced, §7a-clean (dropped the
  single-specimen weight from the bar title). Reader-facing verification_notes
  rewritten (unique specimens, Flensburg attribution via Reynold Junge's
  mintmark). Verified via computed JSON: both under rhinsk_gylden_fod phase 0,
  fraction 1/2, metal gold. SH page unaffected (its fuss_order omits both
  fusses). 3 seed_unsorted Rhinsk Gylden (f2h7g + 2 galster) left as a separate
  classification follow-up.

## Catalog-index sort + range-collapse (later same day)

User: «Schou-діапазони (≥3 поспіль → range), також зроби сортування всіх
індексів» with the messy example `Schou# 26, 28-29, 23-24, 25-26, 36, 9, 10`
→ wants sorted + correctly collapsed.

Implemented in `compute._compute_catalog_groups` (`94d6213`): a nested
`collapse_runs()` replaces the old per-value `sorted(set(...), key=value_key)`.
It expands every value to its integer members FIRST — so existing ranges and
overlapping/adjacent inputs merge (`23-24`+`25-26`+`26` → the int set
{23,24,25,26} → `23-26`), not a messy unsorted overlap. Runs of ≥3 consecutive
ints collapse to `min-max`; 1-2 runs stay individual numbers; non-integer
tokens (letter sub-variants `93A`, dotted `77.1`, register-qualified) never
collapse and keep the existing `value_key` natural order (parent before
sub-variant). Applies to ALL index groups, not just Schou.

User example → `9, 10, 23-26, 28, 29, 36`. Verified on KM-42 (dk-tid-163034):
`Schou# 21, 24, 25, 28, 32-36, 40, 42, 46-48, 51, 52, 56-59, 61, 62, 64, 68,
71-76, 83, 89, 90, 93, 95, 97-99` (was a 38-number flat unsorted-ish list).
2-element runs render as two numbers per the stated ≥3 rule — one-line
threshold flip if ≥2 is wanted later.

## Numista multi-KM support (later same day)

User: «km multi-value + Numista-парсер. роби». §0b — confirmed real (not
theoretical): 5 cache types cite ≥2 distinct KM (406.1/406.2 mint variants;
106/56 + 146/69 across Krause editions; 404,1/404,2/404,3 with European
decimal-COMMAS). Parser took first-wins → 2nd KM silently dropped.

Three-layer fix (`29b5de2`):
- **`numista_canonical.parse_references`** (both API + chrome paths): new
  `_accumulate_ref` builds a deduped list on conflict instead of `setdefault`
  first-wins (§9a). `_normalise_km_number` folds `404,1`→`404.1`. Generalised
  to ALL mapped fields (km/hede/sieg/lange/fr/dav) — blast radius 21/2566 types.
- **`seed_merge` catalog deep-merge**: was «existing sub-key wins» (line 242) →
  silently kept the old scalar km over the fresh list. Now UNIONs list-capable
  sub-fields (`schema_list_catalog_fields() | {km}`) via `_union_cat_values`.
  Frozen-catalog curation still wins (the `key in holds` guard short-circuits).
- **`catalog_codes.normalise_catalog`**: km hygiene step — expand slash-scalar
  (`683.1 / 683.2` → list), comma→dot, case-insensitive dedup. Runs on every
  merge (seeds) AND the absorb blanket pass (final), so it also auto-fixed the
  legacy malformed `km: 683.1 / 683.2` in danish_realm.

Schema `km: str | dict | list[str|KMRef]` already accepts list[str]; display +
the new collapse_runs sort it. Materialized for danish_realm + gottorp_duchy
(re-parse 5 NIDs → build_numista_seed → merge → absorb). Verified render: 112647
«406, 406.1, 406.2, 406.3», 207063 «269, 269.2, 269.3», 319271 «69, 146», 683
«683.1, 683.2», stale comma gone. The richer km also resolved a cross-source dup
(numista-207063 ↔ numismaster-65186; danish_realm 7493→7492, 207063 preserved
3× — de-dup not loss). validate OK.

**Scope cut (deliberate):** `build_numista_seed` rebuilds ALL 16 entity seeds,
but I only re-merged/absorbed the 2 with the KM examples. To keep seed↔unified↔
final consistent I REVERTED the other 14 seed files (code ahead of data). Their
lange/dav/fr accumulation materializes on a deliberate full pipeline run (handoff
🟢). 5 sidecars committed to the cache submodule (`d283dd2a`) + pointer bump.

## Nominal synonym table (later same day)

User: «зроби таблицю» (for the reverted nominal discriminator). §0b — verified
each mapping against the actual data + sources, didn't trust the old notes:
- The notes said «Dobbeldukat» / «Brilledukat»; the DATA has «Dobbelt dukat»
  (two words) and NO «Brilledukat» at all («Brillenthaler» is a distinct coin).
- The real pattern is Danish «Dobbelt X» = «2 X» (dukat / speciedaler / krone /
  skilling / hvid / royalin / mariengroschen / groschen / ungarsk gylden /
  guldkrone), one-word or spaced.
- «Specie / Species / Rigsdaler Specie / Specie norsk / Speciemønt» = Speciedaler.
- BUT «Specie(s)-Dukat» / «Ducat Specie» = Speciedukat (a specie-grade GOLD
  ducat) — NOT a Speciedaler. A broad regression (all 1214 nominals before/after)
  CAUGHT this: my first bare-specie rule false-folded those two to «speciedaler».
  Added a `\bspecies?[- ]?dukat\b → speciedukat` guard BEFORE the bare fold.

7 rules added to `nominal_synonyms.NOMINAL_SYNONYMS` (`5b48840`). The «Dobbelt»
rule is `^(?:1\s+)?dobbelt[- ]?` → «2 » — start-anchored so the 4-fold «4 Dobbelt
Groschen» is left intact (verified). Final regression: 37 nominals fold as
intended, **0 unexpected**; Speciedukat / Portugaløser-fractions / ambiguous-Daler
stay correctly distinct.

This is maintenance-side (the merger's `match_pair` nominal signal), not build.
NOT re-run yet — the table is the prereq; re-introducing the discriminator +
full re-merge is the next step (handoff 🟡). The new synonyms also strengthen the
existing merger's nominal match, so a full re-merge will fold some previously-
split synonym pairs (correct).

## Open threads

- 🔴 290904 PRO RELIGIONE split verdict (awaiting user).
- 🟡 **re-validate-composed_of absorb pass** — now doubly motivated (cleans the
  thinned envelope on KM-42 + the whole sticky-merge class). Designed, dry-run
  pending. Highest leverage.
- 🟡 nominal discriminator — tried + reverted (7/9 splits were synonyms); needs
  the synonym table first.
- 🟡 km multi-value + Numista parser; Rhinsk-Gylden → `rhinsk_gylden_fod` move;
  Schou range-collapse display. (All captured in handoff.)
- 🟢 2 residual `km#` overflow (km excluded from fold — register dict-form);
  `mb` index restart-axis unconfirmed.
- 56 commits unpushed (push only on user «пуш»).

## References

- Commits: `17c7e91` (index code), `75734e6` (index data), `18a5fbe`
  (errata), `7d37a92` (thinning code), `758cfba` (thinning data) + the
  docs commit.
- DECISIONS.md: 3 new 2026-06 entries (scope registry / KMM thinning /
  KMM 275643 errata).
- SOURCES.md §8.3: image+weight cache-detection + thinning note.
- CLAUDE.md + PLAYBOOKS.md PB-8 §«Session end» step 3: mandatory handoff.
- handoff.md: new top section «Catalog-index normalization + KMM thinning».
- Regression tool: `scripts/oneoff/analyze_index_rollout_regressions.py`
  (gitignored).
